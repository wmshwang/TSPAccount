<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.desay_svautomotive.tspaccount">

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true" />

    <!-- 连接网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 读取网络信息状态 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 获取当前wifi状态 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 允许程序改变网络连接状态 -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <!-- 外存储写权限，构建语法需要用到此权限 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 外存储读权限，构建语法需要用到此权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".MyApplication.TSPAccountApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="landscape"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="com.tencent.rdm.uuid" android:value="bca9bdc6-cb7e-9c84-ccf4-aed79328f235" />

        <!-- YOUR_PACKAGE_PATH.CustomPushReceiver需要改为自己的Receiver： -->
        <receiver android:name=".receiver.PushMessageReceiver"
            android:exported="true" >
            <intent-filter>
                <!-- 接收消息透传 -->
                <action android:name="com.tencent.android.tpush.action.PUSH_MESSAGE" />
                <!-- 监听注册、反注册、设置/删除标签、通知被点击等处理结果 -->
                <action android:name="com.tencent.android.tpush.action.FEEDBACK" />
            </intent-filter>
        </receiver>

        <receiver android:name=".receiver.ServiceStartReceiver">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <!--<action android:name="android.media.AUDIO_BECOMING_NOISY" />-->
            </intent-filter>
        </receiver>

        <service
            android:name=".receiver.CallbackAIDLService"
            android:exported="true">
        </service>

        <service android:name=".receiver.InternalService">
        </service>

        <activity android:name=".Activities.VehicleBindingActivity"
            android:configChanges="locale|layoutDirection|keyboard|keyboardHidden|navigation|orientation|screenSize|mcc|mnc|touchscreen|fontScale|uiMode|screenLayout|smallestScreenSize"/>
        <activity android:name=".Activities.MyNewsActivity"
            android:configChanges="locale|layoutDirection|keyboard|keyboardHidden|navigation|orientation|screenSize|mcc|mnc|touchscreen|fontScale|uiMode|screenLayout|smallestScreenSize"/>
        <activity android:name=".Activities.DeviceBindingActivity"
            android:configChanges="locale|layoutDirection|keyboard|keyboardHidden|navigation|orientation|screenSize|mcc|mnc|touchscreen|fontScale|uiMode|screenLayout|smallestScreenSize"/>
        <activity android:name=".Activities.AlreadyPurchasedServiceActivity"
            android:configChanges="locale|layoutDirection|keyboard|keyboardHidden|navigation|orientation|screenSize|mcc|mnc|touchscreen|fontScale|uiMode|screenLayout|smallestScreenSize"/>
        <activity android:name=".Activities.PersonCenterActivity"
            android:configChanges="locale|layoutDirection|keyboard|keyboardHidden|navigation|orientation|screenSize|mcc|mnc|touchscreen|fontScale|uiMode|screenLayout|smallestScreenSize"
            android:theme="@style/Anim_fade">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>